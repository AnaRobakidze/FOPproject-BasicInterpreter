LET N = 29
LET IS_PRIME = 1
FOR I = 2 TO SQR(N)
    IF N MOD I = 0 THEN
        LET IS_PRIME = 0
        EXIT
    END IF
NEXT I
IF IS_PRIME = 1 THEN
    PRINT "Prime"
ELSE
    PRINT "Not Prime"
END IF
